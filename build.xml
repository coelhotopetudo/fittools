<project default="jar" xmlns="antlib:org.apache.tools.ant">

  <property name="project" value="FitTools" />
  <property name="src"     value="src"      />
  <property name="build"   value="build"    />
  <property name="dist"    value="dist"     />
  <property name="lib"     value="lib"      />
  <property name="source"  value="1.5"      />
  <property name="target"  value="1.5"      />
  
  <path id="Classpath">
    <fileset dir="${lib}"/>
  </path>
  
  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>
  
  <target name="clean">
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
  
  <target name="compile" depends="init">
    <mkdir dir="${build}/classes"/>
    <javac srcdir="${src}" destdir="${build}/classes" source="${source}" target="${target}" >
      <classpath refid="Classpath"/>
    </javac>
  </target>
  
  <target name="jar" depends="compile">
    <mkdir dir="${dist}"/>
    <copy todir="${build}/classes/Resources">
      <fileset dir="${src}/Resources"/>
    </copy>
    <jar destfile="${dist}/${project}-${DSTAMP}.jar" basedir="${build}/classes"/>
  </target>
</project>